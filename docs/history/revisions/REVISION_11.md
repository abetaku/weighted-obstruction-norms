# Revision 11 — actual binary primitive costs and obstruction norms

The snapshot contains 609 named theorem/lemma declarations. Verification records are in audit/verification.json; the manuscript is unchanged.

- Both binary examples now use actual State, SupportState, marginal maps, all-degree Cech differentials, and primitive-cost definitions.
- Triangle: twelve-variable equivalence, true pair weights, six marginal equations, closed cocycle, exact cost max(1,2/(3 epsilon)), obstruction norm 2/3, nonzero class, and the unit-primitive threshold.
- Diagonal: four-variable primitive equivalence, exact cost 2/epsilon, nonzero class, norm of a multiple equal to 2 abs(a), and the unit-ball coefficient condition.
- General one-observation H0 and two-observation H1 vanish for every support.
- General augmentation norm and support-extension compatibility are proved.

Remaining: transport the diagonal example's one-dimensionality and connecting-kernel coordinate description to the actual Cech frontend. The finite coordinate window already proves these statements, but this dedicated bridge is not yet completed. This is not a full-paper completion claim.
