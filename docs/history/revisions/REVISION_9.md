# 第9回：相対等長同型と低次数表示の接続

原稿全体は引き続き未完了です。

`CechRelativeIsometry` は、p≥1の相対接続とp=0の追加商の接続を `LinearIsometryEquiv` として構成します。輸送されたノルムが、既に独立に定義・証明した `relativeValue` / `quotientValue` と等しいことも証明しています。

`ObservationAugmentation.restriction_augmentation` は、原稿の次数−1における観測制限が、大域支持測度への `augmentationEquiv` を通して恒等写像になることを証明します。

`CechLowComparison` は、一般Čech実装と旧低次数実装のコチェイン次数0・1・2の線形同型を構成し、増大写像および二つの微分との可換性を証明します。コサイクルと境界部分空間を同定することで、元のH⁰・H¹と旧表示のH⁰・H¹の線形同型を得ます。

二値例の専用有限座標表示との接続と、付録の順序複体比較はまだ完成していません。この改訂の低次数比較を、そのまま二値例の一般実装への接続として数えてはいません。
